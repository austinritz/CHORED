import '../assets/pages/UserTasksPage.css';

/* UserTasksPage component
- Similar to HouseholdPage, but only shows the tasks for which the current user is next in the queue
- Each task in the calendar is clickable and shows the task details
- Each task in the calendar is color coded to indicate the household it belongs to
*/
const UserTasksPage = () => {
  return (
    <div className="UserTasksPage">
    </div>
  );
};

export default UserTasksPage;