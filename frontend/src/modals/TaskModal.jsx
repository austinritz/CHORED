import '../assets/modals/TaskModal.css';

/* TaskModal component
- Shows a modal with the details of a task
- Modal includes the task name, description, user queue, recurrence, and reminder buffer time
- Modal includes a button to edit the task (updates the modal to show input fields)
- Modal includes a button to move the task to the next user in the queue
- Modal includes a button to complete the task
- Modal includes a button to skip the task
- Modal includes a button to delete the task
*/
const TaskModal = () => {
  return (
    <div className="TaskModal">
    </div>
  );
};

export default TaskModal;